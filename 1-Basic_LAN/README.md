# Lab 1: Basic LAN Setup

## Description
A simple local network consisting of 4 devices connected to a switch with an IP range of `192.168.1.x`.

## Testing Output
In the `Test.png` image, the `ipconfig` and `ping` commands were executed. A broadcast ping (`ping 192.168.1.255`) was successfully used to verify connectivity to all devices on the network simultaneously.
